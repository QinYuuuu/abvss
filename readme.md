### init
$ go mod init abvss

$ go mod tidy

### run 4 nodes
in /cmd/testdkg

open 4 terminal, -path = the path of the abvss folder

$ go run main.go -id=0 -path=""

$ go run main.go -id=1 -path=""

$ go run main.go -id=2 -path=""

$ go run main.go -id=3 -path=""
